Look at all uncommitted changes with `git diff` and `git status`.

Based on what changed, write a short clear commit message in English (max 72 chars) that describes what was done.

Then:
1. `git add -A`
2. `git commit -m "<your message>"`
3. `git push`

Do not ask for confirmation. Just do it.
